spring:
  datasource:
    url: jdbc:mysql://localhost:3306/big?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: demo
mybatis:
  mapper-locations: classpath:dao/*.xml
  type-aliases-package: com.example.demo.entity
server:
  port: 1145

